type Activities {
  activeSeconds: Float!
  app: String!
  category(where: CategoriesFilters): Categories
  categoryId: String
  categorySource: ActivitiesCategorySourceEnum
  closedAt: DateTime
  context: String

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  device(where: DevicesFilters): Devices
  deviceId: String!
  id: String!
  lastActiveAt: DateTime!
  rolledUp: Boolean!
  startedAt: DateTime!
  title: String
}

enum ActivitiesCategorySourceEnum {
  """Value: manual"""
  manual

  """Value: rule"""
  rule
}

input ActivitiesCategorySourceEnumFilter {
  """Every branch matches"""
  AND: [ActivitiesCategorySourceEnumFilter!]

  """Negates the nested operators"""
  NOT: ActivitiesCategorySourceEnumFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [ActivitiesCategorySourceEnumFilter!]

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """Equal to"""
  eq: ActivitiesCategorySourceEnum

  """Greater than"""
  gt: ActivitiesCategorySourceEnum

  """Greater than or equal to"""
  gte: ActivitiesCategorySourceEnum

  """Case-insensitive `contains`."""
  iContains: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String
  ilike: String

  """Matches any one of these values (SQL `IN`)"""
  inArray: [ActivitiesCategorySourceEnum!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean
  like: String

  """Less than"""
  lt: ActivitiesCategorySourceEnum

  """Less than or equal to"""
  lte: ActivitiesCategorySourceEnum

  """Not equal to"""
  ne: ActivitiesCategorySourceEnum
  notIlike: String

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [ActivitiesCategorySourceEnum!]
  notLike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String
}

input ActivitiesFilters {
  """Every branch matches"""
  AND: [ActivitiesFilters!]

  """Negates the nested filters"""
  NOT: ActivitiesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [ActivitiesFilters!]
  activeSeconds: FloatFilter
  app: StringFilter

  """Matches rows whose category matches these filters"""
  category: CategoriesFilters
  categoryId: StringFilter
  categorySource: ActivitiesCategorySourceEnumFilter
  closedAt: DateTimeFilter
  context: StringFilter

  """Matches rows whose device matches these filters"""
  device: DevicesFilters
  deviceId: StringFilter
  id: StringFilter
  lastActiveAt: DateTimeFilter
  rolledUp: BooleanFilter
  startedAt: DateTimeFilter
  title: StringFilter
}

input ActivitiesListRelationFilter {
  """Every related row matches"""
  every: ActivitiesFilters

  """No related row matches"""
  none: ActivitiesFilters

  """At least one related row matches"""
  some: ActivitiesFilters
}

input ActivitiesOrderBy {
  activeSeconds: InnerOrder
  app: InnerOrder

  """Order by columns of the related category row"""
  category: CategoriesOrderBy
  categoryId: InnerOrder
  categorySource: InnerOrder
  closedAt: InnerOrder
  context: InnerOrder

  """Order by columns of the related device row"""
  device: DevicesOrderBy
  deviceId: InnerOrder
  id: InnerOrder
  lastActiveAt: InnerOrder
  rolledUp: InnerOrder
  startedAt: InnerOrder
  title: InnerOrder
}

type AppContextSummary {
  app: String!
  context: String
  seconds: Float!
}

type AuthSession {
  token: String!
  userId: String!
}

input BooleanFilter {
  """Every branch matches"""
  AND: [BooleanFilter!]

  """Negates the nested operators"""
  NOT: BooleanFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [BooleanFilter!]

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """Equal to"""
  eq: Boolean

  """Greater than"""
  gt: Boolean

  """Greater than or equal to"""
  gte: Boolean

  """Case-insensitive `contains`."""
  iContains: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String
  ilike: String

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Boolean!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean
  like: String

  """Less than"""
  lt: Boolean

  """Less than or equal to"""
  lte: Boolean

  """Not equal to"""
  ne: Boolean
  notIlike: String

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Boolean!]
  notLike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String
}

type Categories {
  activities(
    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: ActivitiesOrderBy
    where: ActivitiesFilters
  ): [Activities!]!
  color: String
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  id: String!
  name: String!
  rules(
    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: CategoryRulesOrderBy
    where: CategoryRulesFilters
  ): [CategoryRules!]!
  userId: String!
}

input CategoriesFilters {
  """Every branch matches"""
  AND: [CategoriesFilters!]

  """Negates the nested filters"""
  NOT: CategoriesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CategoriesFilters!]
  activities: ActivitiesListRelationFilter
  color: StringFilter
  createdAt: DateTimeFilter
  id: StringFilter
  name: StringFilter
  rules: CategoryRulesListRelationFilter
  userId: StringFilter
}

input CategoriesOrderBy {
  color: InnerOrder
  createdAt: InnerOrder
  id: InnerOrder
  name: InnerOrder
  userId: InnerOrder
}

type CategoryDaySummary {
  categoryId: String
  color: String
  day: String!
  name: String
  seconds: Float!
}

type CategoryRules {
  appPattern: String
  category(where: CategoriesFilters): Categories
  categoryId: String!
  contextPattern: String
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  id: String!
  priority: Int!
  titlePattern: String
  userId: String!
}

input CategoryRulesFilters {
  """Every branch matches"""
  AND: [CategoryRulesFilters!]

  """Negates the nested filters"""
  NOT: CategoryRulesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CategoryRulesFilters!]
  appPattern: StringFilter

  """Matches rows whose category matches these filters"""
  category: CategoriesFilters
  categoryId: StringFilter
  contextPattern: StringFilter
  createdAt: DateTimeFilter
  id: StringFilter
  priority: IntFilter
  titlePattern: StringFilter
  userId: StringFilter
}

input CategoryRulesListRelationFilter {
  """Every related row matches"""
  every: CategoryRulesFilters

  """No related row matches"""
  none: CategoryRulesFilters

  """At least one related row matches"""
  some: CategoryRulesFilters
}

input CategoryRulesOrderBy {
  appPattern: InnerOrder

  """Order by columns of the related category row"""
  category: CategoriesOrderBy
  categoryId: InnerOrder
  contextPattern: InnerOrder
  createdAt: InnerOrder
  id: InnerOrder
  priority: InnerOrder
  titlePattern: InnerOrder
  userId: InnerOrder
}

type ContextRules {
  appPattern: String
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  id: String!
  priority: Int!
  titlePattern: String!
  userId: String!
}

input ContextRulesFilters {
  """Every branch matches"""
  AND: [ContextRulesFilters!]

  """Negates the nested filters"""
  NOT: ContextRulesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [ContextRulesFilters!]
  appPattern: StringFilter
  createdAt: DateTimeFilter
  id: StringFilter
  priority: IntFilter
  titlePattern: StringFilter
  userId: StringFilter
}

input ContextRulesOrderBy {
  appPattern: InnerOrder
  createdAt: InnerOrder
  id: InnerOrder
  priority: InnerOrder
  titlePattern: InnerOrder
  userId: InnerOrder
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

input DateTimeFilter {
  """Every branch matches"""
  AND: [DateTimeFilter!]

  """Negates the nested operators"""
  NOT: DateTimeFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [DateTimeFilter!]

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """Equal to"""
  eq: DateTime

  """Greater than"""
  gt: DateTime

  """Greater than or equal to"""
  gte: DateTime

  """Case-insensitive `contains`."""
  iContains: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String
  ilike: String

  """Matches any one of these values (SQL `IN`)"""
  inArray: [DateTime!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean
  like: String

  """Less than"""
  lt: DateTime

  """Less than or equal to"""
  lte: DateTime

  """Not equal to"""
  ne: DateTime
  notIlike: String

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [DateTime!]
  notLike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String
}

type DeviceRegistration {
  apiKey: String!
  device: Devices!
}

type DeviceSummary {
  deviceId: String!
  name: String!
  platform: String!
  seconds: Float!
}

type Devices {
  activities(
    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: ActivitiesOrderBy
    where: ActivitiesFilters
  ): [Activities!]!
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  id: String!
  lastSeenAt: DateTime
  name: String!
  platform: DevicesPlatformEnum!
  summaries(
    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: SummariesOrderBy
    where: SummariesFilters
  ): [Summaries!]!
  userId: String!
}

input DevicesFilters {
  """Every branch matches"""
  AND: [DevicesFilters!]

  """Negates the nested filters"""
  NOT: DevicesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [DevicesFilters!]
  activities: ActivitiesListRelationFilter
  createdAt: DateTimeFilter
  id: StringFilter
  lastSeenAt: DateTimeFilter
  name: StringFilter
  platform: DevicesPlatformEnumFilter
  summaries: SummariesListRelationFilter
  userId: StringFilter
}

input DevicesOrderBy {
  createdAt: InnerOrder
  id: InnerOrder
  lastSeenAt: InnerOrder
  name: InnerOrder
  platform: InnerOrder
  userId: InnerOrder
}

enum DevicesPlatformEnum {
  """Value: android"""
  android

  """Value: linux"""
  linux

  """Value: macos"""
  macos

  """Value: windows"""
  windows
}

input DevicesPlatformEnumFilter {
  """Every branch matches"""
  AND: [DevicesPlatformEnumFilter!]

  """Negates the nested operators"""
  NOT: DevicesPlatformEnumFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [DevicesPlatformEnumFilter!]

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """Equal to"""
  eq: DevicesPlatformEnum

  """Greater than"""
  gt: DevicesPlatformEnum

  """Greater than or equal to"""
  gte: DevicesPlatformEnum

  """Case-insensitive `contains`."""
  iContains: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String
  ilike: String

  """Matches any one of these values (SQL `IN`)"""
  inArray: [DevicesPlatformEnum!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean
  like: String

  """Less than"""
  lt: DevicesPlatformEnum

  """Less than or equal to"""
  lte: DevicesPlatformEnum

  """Not equal to"""
  ne: DevicesPlatformEnum
  notIlike: String

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [DevicesPlatformEnum!]
  notLike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String
}

input FloatFilter {
  """Every branch matches"""
  AND: [FloatFilter!]

  """Negates the nested operators"""
  NOT: FloatFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [FloatFilter!]

  """Equal to"""
  eq: Float

  """Greater than"""
  gt: Float

  """Greater than or equal to"""
  gte: Float

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Float!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """Less than"""
  lt: Float

  """Less than or equal to"""
  lte: Float

  """Not equal to"""
  ne: Float

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Float!]
}

input InnerOrder {
  direction: OrderDirection!

  """
  Sort by this column's position in the `inArray` list the same request's `where` gives it, rather than by the column's own value — `direction: asc` keeps the list's order, `desc` reverses it. Requires an `inArray` filter on the same column at the top level of `where`, and cannot be combined with `after` or `distinct`.
  """
  matchFilterOrder: Boolean

  """
  Where NULL values sort. Defaults to the database's own rule (PostgreSQL: last on asc, first on desc; MySQL/SQLite: first on asc, last on desc)
  """
  nulls: OrderNulls

  """Priority of current field"""
  priority: Int!
}

input IntFilter {
  """Every branch matches"""
  AND: [IntFilter!]

  """Negates the nested operators"""
  NOT: IntFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [IntFilter!]

  """Equal to"""
  eq: Int

  """Greater than"""
  gt: Int

  """Greater than or equal to"""
  gte: Int

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Int!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """Less than"""
  lt: Int

  """Less than or equal to"""
  lte: Int

  """Not equal to"""
  ne: Int

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Int!]
}

type MagicLinkRequest {
  ok: Boolean!
  token: String
}

type MergeRules {
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  fromApp: String!
  fromContext: String
  id: String!
  toApp: String!
  toContext: String
  userId: String!
}

input MergeRulesFilters {
  """Every branch matches"""
  AND: [MergeRulesFilters!]

  """Negates the nested filters"""
  NOT: MergeRulesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [MergeRulesFilters!]
  createdAt: DateTimeFilter
  fromApp: StringFilter
  fromContext: StringFilter
  id: StringFilter
  toApp: StringFilter
  toContext: StringFilter
  userId: StringFilter
}

input MergeRulesOrderBy {
  createdAt: InnerOrder
  fromApp: InnerOrder
  fromContext: InnerOrder
  id: InnerOrder
  toApp: InnerOrder
  toContext: InnerOrder
  userId: InnerOrder
}

type Mutation {
  applyCategoryRules: Int!
  applyMergeRules: Int!
  assignActivity(activityId: String!, categoryId: String): Activities!
  createCategory(color: String, name: String!): Categories!
  createCategoryRule(appPattern: String, categoryId: String!, contextPattern: String, priority: Int, titlePattern: String): CategoryRules!
  createContextRule(appPattern: String, priority: Int, titlePattern: String!): ContextRules!
  createMergeRule(fromApp: String!, fromContext: String, toApp: String!, toContext: String): MergeRules!
  deleteCategory(id: String!): Boolean!
  deleteCategoryRule(id: String!): Boolean!
  deleteContextRule(id: String!): Boolean!
  deleteDevice(id: String!): Boolean!
  deleteMergeRule(id: String!): Boolean!
  mergeDevice(id: String!, intoId: String!): Devices!
  recordPing(app: String, capturedAt: String!, context: String, deviceId: String, idleSeconds: Int!, title: String): Activities
  recordPings(deviceId: String, pings: [PingInput!]!): Int!
  registerDevice(name: String!, platform: String!): DeviceRegistration!
  renameDevice(id: String!, name: String!): Devices!
  requestMagicLink(email: String!): MagicLinkRequest!
  rotateDeviceKey(id: String!): DeviceRegistration!
  sessionFromDeviceKey: AuthSession!
  signIn(email: String!, password: String!): AuthSession!
  signOut: Boolean!
  signUp(email: String!, name: String!, password: String!): AuthSession!
  updateCategoryRule(appPattern: String, categoryId: String!, contextPattern: String, id: String!, priority: Int, titlePattern: String): CategoryRules!
  updateContextRule(appPattern: String, id: String!, priority: Int, titlePattern: String!): ContextRules!
  verifyMagicLink(token: String!): AuthSession!
}

"""Order by direction"""
enum OrderDirection {
  """Ascending order"""
  asc

  """Descending order"""
  desc
}

"""Where NULL values sort relative to non-NULL values"""
enum OrderNulls {
  """NULL values sort before all non-NULL values"""
  first

  """NULL values sort after all non-NULL values"""
  last
}

"""A stateless report of what a device looked like at one instant."""
input PingInput {
  app: String
  capturedAt: String!
  context: String
  idleSeconds: Int!
  title: String
}

type Query {
  activities(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: ActivitiesOrderBy
    where: ActivitiesFilters
  ): [Activities!]!

  """
  Time per app and context over the window — the same seconds as
  `categorySummary`, grouped by what was actually on screen rather than by
  category. Not per-day: one row per app/context pair for the whole window.
  """
  appSummary(
    """Limit to one device (see `devices`). Omitted means every device."""
    deviceId: String

    """
    Start of the window, inclusive. A calendar date, `YYYY-MM-DD`, read in the server's time zone.
    """
    from: String!

    """
    End of the window, EXCLUSIVE — pass the day after the last one you want.
    """
    to: String!
  ): [AppContextSummary!]!
  categories(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: CategoriesOrderBy
    where: CategoriesFilters
  ): [Categories!]!
  categoryRules(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: CategoryRulesOrderBy
    where: CategoryRulesFilters
  ): [CategoryRules!]!

  """
  Time per category per day — the answer to "what did I spend last week on?".
  
  Each aggregate below stitches the precomputed `summaries` rows together with
  a live aggregation over whatever the 15-minute rollup hasn't claimed yet, so
  today is never missing.
  """
  categorySummary(
    """Limit to one device (see `devices`). Omitted means every device."""
    deviceId: String

    """
    Start of the window, inclusive. A calendar date, `YYYY-MM-DD`, read in the server's time zone.
    """
    from: String!

    """
    End of the window, EXCLUSIVE — pass the day after the last one you want.
    """
    to: String!
  ): [CategoryDaySummary!]!
  contextRules(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: ContextRulesOrderBy
    where: ContextRulesFilters
  ): [ContextRules!]!

  """
  Time per device over the window — which machine or phone the time came from.
  """
  deviceSummary(
    """
    Start of the window, inclusive. A calendar date, `YYYY-MM-DD`, read in the server's time zone.
    """
    from: String!

    """
    End of the window, EXCLUSIVE — pass the day after the last one you want.
    """
    to: String!
  ): [DeviceSummary!]!
  devices(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: DevicesOrderBy
    where: DevicesFilters
  ): [Devices!]!

  """The caller's user id, or null when the request is anonymous."""
  me: String
  mergeRules(
    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    limit: Int
    offset: Int
    orderBy: MergeRulesOrderBy
    where: MergeRulesFilters
  ): [MergeRules!]!
}

input StringFilter {
  """Every branch matches"""
  AND: [StringFilter!]

  """Negates the nested operators"""
  NOT: StringFilter

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [StringFilter!]

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """Equal to"""
  eq: String

  """Greater than"""
  gt: String

  """Greater than or equal to"""
  gte: String

  """Case-insensitive `contains`."""
  iContains: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String
  ilike: String

  """Matches any one of these values (SQL `IN`)"""
  inArray: [String!]

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """When true, matches rows where the column is NULL"""
  isNull: Boolean
  like: String

  """Less than"""
  lt: String

  """Less than or equal to"""
  lte: String

  """Not equal to"""
  ne: String
  notIlike: String

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [String!]
  notLike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String
}

type Summaries {
  app: String!
  categoryId: String
  context: String

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  day: String!
  device(where: DevicesFilters): Devices
  deviceId: String!
  id: String!
  seconds: Float!
}

input SummariesFilters {
  """Every branch matches"""
  AND: [SummariesFilters!]

  """Negates the nested filters"""
  NOT: SummariesFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [SummariesFilters!]
  app: StringFilter
  categoryId: StringFilter
  context: StringFilter
  day: StringFilter

  """Matches rows whose device matches these filters"""
  device: DevicesFilters
  deviceId: StringFilter
  id: StringFilter
  seconds: FloatFilter
}

input SummariesListRelationFilter {
  """Every related row matches"""
  every: SummariesFilters

  """No related row matches"""
  none: SummariesFilters

  """At least one related row matches"""
  some: SummariesFilters
}

input SummariesOrderBy {
  app: InnerOrder
  categoryId: InnerOrder
  context: InnerOrder
  day: InnerOrder

  """Order by columns of the related device row"""
  device: DevicesOrderBy
  deviceId: InnerOrder
  id: InnerOrder
  seconds: InnerOrder
}
